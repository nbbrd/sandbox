language: java
jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

deploy:
  - provider: script
    script: echo hello world
    skip_cleanup: true
    on:
      branch: master
      jdk: oraclejdk8
      condition: $TRAVIS_PULL_REQUEST = "false"
      