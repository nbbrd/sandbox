language: java
jdk:
  - oraclejdk8
  - openjdk11

cache:
  directories:
    - $HOME/.m2

env:
  - MAIN_JDK=oraclejdk8
  - MAIN_REPO=nbbrd/sandbox

deploy:
  # Maven snapshots from develop branch
  - provider: script
    script: echo hello snapshots
    skip_cleanup: true
    on:
      branch: develop
      repo: $MAIN_REPO
      jdk: $MAIN_JDK
      condition: $TRAVIS_PULL_REQUEST = "false"

  # Maven releases from master branch
  - provider: script
    script: echo hello releases
    skip_cleanup: true
    on:
      branch: master
      repo: $MAIN_REPO
      jdk: $MAIN_JDK
      condition: $TRAVIS_PULL_REQUEST = "false"
